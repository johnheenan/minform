---
layout: default.njk
---
{% if page.url.startsWith('/blog/') and page.url != '/blog/' %}
<h1>{{ title }}</h1>

<h2>{{ description }}</h2>
{%- set prevnext = collections.all | filterBlogs | prevnextPost(page.url) -%}
{%- if prevnext.previous %}{% set prevLink %}<- <a href="{{ prevnext.previous.url }}">{{ prevnext.previous.title | truncate(20) }}</a>,&nbsp;&nbsp{% endset %}{% endif %}
{% if prevnext.next %}{% set nextLink %}<a href="{{ prevnext.next.url }}">{{ prevnext.next.title | truncate(20) }}</a> ->{% endset %}{% endif %}<br>

{% displayDate date %},&nbsp;&nbsp;&nbsp;{{ prevLink | safe }} {{ nextLink | safe }}<br>
{% for tag in tags | sortAlphabetically %}
	{% set tagUrl %}/tags/{{ tag | slugify }}/{% endset %}
	[<a href="{{ tagUrl }}">{{ tag }}</a>]
{% endfor %}<p>
{% endif %}

{{ content | safe }}

{{ prevLink | safe }} {{ nextLink | safe }}

