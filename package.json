{
  "name": "Minform",
  "version": "0.5.1",
  "description": "Website Template Minimalist Forms",
  "private": true,
  "type": "module",
  "workspaces": [
    "do"
  ],
  "scripts": {
    "do": "ELTY_OPTIONS=--input=./site-1 npm -w do run",
    "start": "npm run do -- start:with-tw",
    "stage": "npm run do -- stage",
    "build": "npm run do -- build",
    "dlbaseblog": "curl -L https://github.com/11ty/eleventy-base-blog/tarball/main | tar -xz --strip-components=2 --wildcards -C ./site-1 '*/content/blog/*.md' '*/content/blog/*.png' && sed -i 's/\\.md//g;s/blog\\/third/\\/blog\\/third/' ./site-1/blog/secondpost.md",
    "upload": "export $(cat .env | sed '/^ *#/d' | xargs) && [ -z \"$SITE1_REMOTE_DEST\" ] && echo '$SITE1_REMOTE_DEST' not in .env, see .env.sample for example || rsync -avzr --delete _site/ $SITE1_REMOTE_DEST"
  },
  "author": "John Heenan and others",
  "license": "MIT",
  "devDependencies": {
    "dotenv": "^17.4.2",
    "eleventy-auto-cache-buster": "^0.9.2",
    "prettier-plugin-jinja-template": "^2.2.0",
    "serve": "^14.2.6"
  },
  "dependencies": {
    "@11ty/eleventy": "4.0.0-alpha.7",
    "@11ty/eleventy-img": "^6.0.4",
    "@anyblades/eleventy-blades-base": "^0.4.5",
    "@anyblades/pico": "^2.2.2",
    "@tailwindcss/cli": "^4.3.0",
    "date-fns": "^4.3.0"
  }
}
